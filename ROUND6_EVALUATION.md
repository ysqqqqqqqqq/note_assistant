# 第六轮：真实回答评测与谨慎模式故障修复

## 结论

修复谨慎模式的确定性故障漏洞：有拒答阈值时，重排模型缺失、推理异常、非有限分数、结果数量错误均停止生成，返回 verification_unavailable；普通无阈值模式仍保留检索回退。语言不匹配也受保护。界面与后端将校验服务故障和资料不足分开提示。

真实智谱试跑说明：glm-4-flash-250414，12 道合成题，两种配置共24条结果，其中21条实际请求模型，3条由无证据规则直接拒答。接口凭据只在进程中使用，结果文件无凭据。数据来自已使用过的冻结测试集，是小规模回归，不是新盲测。每种题型2题，覆盖直接问法、同义表达、否定、流程、版本比较和无答案。

## 两种配置比较（不是代码修复前后准确率）

| 指标 | Hybrid | 谨慎模式（阈值0） |
|---|---:|---:|
| 核心回答正确（Codex逐条复核） | 12/12 | 10/12 |
| 有答案题正确 | 10/10 | 8/10 |
| 无答案题正确拒答 | 2/2 | 2/2 |
| 实际模型调用 | 12 | 9 |
| 模型输出含合法单编号引用 | 12/12 | 8/9 |
| 实际模型请求中位耗时（毫秒） | 883 | 583 |
| 总 tokens（接口返回） | 3311 | 1760 |

核心正确不要求所有附带陈述都完美。引用指标仅检查 [S1] 形式编号，不验证事实支持或组合引用 [S1, S2] 的完整性。没有把这些计数冒充 faithfulness/answer relevance 的标准评分；这两项仍需独立评审和明确标注规则。

## 代码修复前后：故障注入

四种故障各1个固定用例：修复前4/4继续返回证据，修复后0/4放行，4/4返回校验不可用。另有接口测试确认只执行术语提取，不执行后续解释调用；模型健康时行为保留。并非通过这些故障用例证明真实回答准确率提升。

## 错题与后续优先级

- cautious / rewrite-paraphrase：错误拒答。原库有“两条改写”，阈值把候选全部过滤。
- cautious / citation-cross_version：答偏且不完整。未召回引用v2，却回答索引和批处理v1；引用编号存在不能代表答对。
- cautious / index-procedure：缺少引用。关键内容正确，但没有任何来源编号。
- hybrid / filter-unanswerable：证据描述不准确。正确拒答，但声称三个来源仅描述过滤内容；第三个其实是向量编码流程。

下一步优先做版本比较的证据覆盖检查和阈值校准，再检查逐句引用与无关事实；不直接为了这12题调整阈值。两道版本题沿用了“数量或时长”模板，会引出额外的时长讨论，新评测集应使用更明确的自然问题。本轮生成提示为评测专用直接问答，不是网页的术语提取→短批注完整流程，因此不能把结果称为整站端到端准确率。

## 逐题输出

### index-direct · hybrid

问题：索引规范v2：增量索引的触发间隔是多少？

参考：增量索引每九分钟触发一次。

实际回答：增量索引每九分钟触发一次 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### index-direct · cautious

问题：索引规范v2：增量索引的触发间隔是多少？

参考：增量索引每九分钟触发一次。

实际回答：增量索引每九分钟触发一次 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### export-direct · hybrid

问题：导出规范v2：一次最多导出多少张批注卡片？

参考：单次导出最多包含三十张批注卡片。

实际回答：根据资料[S1]，导出规范v2中，单次导出最多包含三十张批注卡片。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### export-direct · cautious

问题：导出规范v2：一次最多导出多少张批注卡片？

参考：单次导出最多包含三十张批注卡片。

实际回答：根据资料[S1]，导出规范v2中，单次导出最多包含三十张批注卡片。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### citation-paraphrase · hybrid

问题：回答旁边最多列出几个参考出处？

参考：每条答案最多展示五个来源。

实际回答：最多展示五个来源 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### citation-paraphrase · cautious

问题：回答旁边最多列出几个参考出处？

参考：每条答案最多展示五个来源。

实际回答：最多列出五个参考出处 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rewrite-paraphrase · hybrid

问题：系统会为一条检索问题准备多少种额外问法？

参考：每个问题最多生成两条改写。

实际回答：根据资料[S1]，系统为一条检索问题最多准备两条额外问法。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rewrite-paraphrase · cautious

问题：系统会为一条检索问题准备多少种额外问法？

参考：每个问题最多生成两条改写。

实际回答：知识库证据不足，无法回答。

复核：错误拒答。原库有“两条改写”，阈值把候选全部过滤。

### filter-negation · hybrid

问题：没有过滤条件时能跳过权限检查吗？

参考：过滤条件为空不代表跳过权限检查。

实际回答：不能。过滤条件为空不代表跳过权限检查 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### filter-negation · cautious

问题：没有过滤条件时能跳过权限检查吗？

参考：过滤条件为空不代表跳过权限检查。

实际回答：不能。过滤条件为空不代表跳过权限检查 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rerank-negation · hybrid

问题：重排出错会自动发送到外部搜索吗？

参考：重排错误不会自动改用外部搜索引擎。

实际回答：不会。重排错误不会自动改用外部搜索引擎 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rerank-negation · cautious

问题：重排出错会自动发送到外部搜索吗？

参考：重排错误不会自动改用外部搜索引擎。

实际回答：不会。重排错误不会自动改用外部搜索引擎 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### export-procedure · hybrid

问题：导出过程中何时替换目标文件？

参考：导出先保存临时文件，校验成功后才替换目标文件。

实际回答：导出过程中，先保存临时文件，校验成功后才替换目标文件 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### export-procedure · cautious

问题：导出过程中何时替换目标文件？

参考：导出先保存临时文件，校验成功后才替换目标文件。

实际回答：导出过程中，先保存临时文件，校验成功后才替换目标文件 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### index-procedure · hybrid

问题：新索引在什么条件下切换，切换后广播什么？

参考：新索引通过完整性检查后，才原子切换并广播版本号。

实际回答：新索引通过完整性检查后，原子切换并广播版本号 [S1]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### index-procedure · cautious

问题：新索引在什么条件下切换，切换后广播什么？

参考：新索引通过完整性检查后，才原子切换并广播版本号。

实际回答：新索引通过完整性检查后，原子切换并广播版本号。操作失败会保留诊断记录。

复核：缺少引用。关键内容正确，但没有任何来源编号。

### rewrite-cross_version · hybrid

问题：查询改写的v1和v2数量或时长规定分别是什么？

参考：每个问题最多生成一条改写。 每个问题最多生成两条改写。

实际回答：v1：每个问题最多生成一条改写 [S1]。
v2：每个问题最多生成两条改写，且不得改变原问题中的否定条件 [S2]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rewrite-cross_version · cautious

问题：查询改写的v1和v2数量或时长规定分别是什么？

参考：每个问题最多生成一条改写。 每个问题最多生成两条改写。

实际回答：查询改写v1的规定是每个问题最多生成一条改写 [S1]；v2的规定是每个问题最多生成两条改写，且不得改变原问题中的否定条件 [S2]。资料未提供v1和v2关于时长或触发频率的规定 [S1, S2, S3]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### citation-cross_version · hybrid

问题：引用的v1和v2数量或时长规定分别是什么？

参考：每条答案最多展示两个来源。 每条答案最多展示五个来源。

实际回答：v1规定每条答案最多展示两个来源 [S1]；v2规定每条答案最多展示五个来源 [S2]。资料未提及v1和v2关于时长或触发频率的规定 [S1, S2, S3]。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### citation-cross_version · cautious

问题：引用的v1和v2数量或时长规定分别是什么？

参考：每条答案最多展示两个来源。 每条答案最多展示五个来源。

实际回答：引用v1规定每条答案最多展示两个来源 [S1]。索引v1规定增量索引每两分钟触发一次 [S2]。批处理v1规定单批任务最多包含六个文档 [S3]。

复核：答偏且不完整。未召回引用v2，却回答索引和批处理v1；引用编号存在不能代表答对。

### rerank-unanswerable · hybrid

问题：重排服务的负责人手机号码是什么？

参考：材料没有提供该信息。

实际回答：无法确定。资料中未提供重排服务的负责人手机号码信息。[S1] 和 [S3] 均未约定负责人联系方式，[S2] 仅说明重排规范。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### rerank-unanswerable · cautious

问题：重排服务的负责人手机号码是什么？

参考：材料没有提供该信息。

实际回答：知识库证据不足，无法回答。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

### filter-unanswerable · hybrid

问题：过滤模块的源代码有多少行？

参考：材料没有提供该信息。

实际回答：无法确定。资料中未提及过滤模块的源代码行数。[S1]、[S2]和[S3]仅描述了过滤规范和操作流程，未提供关于源代码行数的信息。

复核：证据描述不准确。正确拒答，但声称三个来源仅描述过滤内容；第三个其实是向量编码流程。

### filter-unanswerable · cautious

问题：过滤模块的源代码有多少行？

参考：材料没有提供该信息。

实际回答：知识库证据不足，无法回答。

复核：核心内容正确。对照参考答案和检索原文，关键内容符合；不等于每句均通过独立事实裁判。

## 复现与文件

answer_pilot.py 从 ZHIPU_API_KEY 环境变量读取凭据；参数 --dense、--reranker 为本地模型目录，--out 为结果目录。测试会实际调用智谱；运行前自行配置凭据。完整请求结果含证据原文，见 answers.json；评审见 review.json；冻结选题见 selection.json；故障对比见 fault-comparison.json。

本轮未改变现有默认模型或用户 API 设置。测试模型、向量模型均沿用现有版本。
Validation: 41 Python + 10 JavaScript tests passed; real BGE HTTP smoke passed.
